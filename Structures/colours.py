import enum

class Colour(enum.Enum):
    """Enum for defining colours used in trait levels"""

    BRONZE = "BRONZE"
    SILVER = "SILVER"
    GOLD = "GOLD"
    PRISMATIC = "PRISMATIC"
    DISABLED = "DISABLED"